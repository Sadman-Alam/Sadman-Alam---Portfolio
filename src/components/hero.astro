---
const aboutMe = {
  name: "About Me",
  description: [
    `I'm <span class="shiny-sec">Sadman Alam</span>, a designer and developer who loves blending creativity with technology. By day, I help bring ideas to life through clean, user-friendly websites and branding. But beyond client projects, I'm driven by a curiosity to solve everyday problems - the kind of challenges that spark my indie maker spirit.`,
    "I enjoy sharing knowledge and exploring new design tools. Whether it's diving into no-code platforms, experimenting with UX concepts, or posting behind-the-scenes of my creative process, I enjoy connecting with others who share my enthusiasm.",
    "When I'm not designing or coding, you'll probably find me capturing the world through my camera lens or savoring a quiet moment with a good cup of coffee. I believe creativity thrives in the little moments, and the stories around me always inspire me.",
  ],
  imageUrl: "/profile2.jpg",
};
---

<section
  id="hero"
  class="pt-8 md:pt-36 pb-8"
  style="background: transparent; color: var(--white);"
>
  <div class="max-w-5xl mx-auto px-2 sm:px-3 md:px-6 flex flex-col-reverse md:flex-row items-center gap-6 md:gap-10">
    
    <!-- Text -->
    <div class="w-full md:w-1/2 space-y-4 text-left">
      <h1 class="text-2xl sm:text-3xl md:text-4xl font-bold">{aboutMe.name}</h1>

      {aboutMe.description.map((paragraph) => (
        paragraph.includes("<") ? (
          <p
            class="text-white-icon text-sm sm:text-base leading-relaxed"
            set:html={paragraph}
          ></p>
        ) : (
          <p
            class="text-white-icon text-sm sm:text-base leading-relaxed"
          >
            {paragraph}
          </p>
        )
      ))}
    </div>

    <!-- Image -->
    <div class="w-full md:w-1/2 flex justify-center">
      <div
        class="w-40 sm:w-52 md:w-80 aspect-[4/5] rounded-lg shadow-lg overflow-hidden"
      >
        <img
          src={aboutMe.imageUrl}
          alt="Photo of Muhammad Sadman Alam"
          class="w-full h-full object-cover"
          loading="lazy"
          decoding="async"
          width="1000"
          height="1000"
        />
      </div>
    </div>
  </div>
</section>

<style is:global>
  .text-white-icon {
    color: var(--white-icon);
  }

  .shiny-sec {
    background: linear-gradient(135deg, var(--sec) 25%, #cecece 50%, var(--sec) 75%);
    background-size: 400% 100%;
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
    animation: shine 2.5s linear infinite;
  }

  @keyframes shine {
    0% {
      background-position: 100% 50%;
    }
    30%,
    70% {
      background-position: 0% 50%;
    }
    100% {
      background-position: -100% 50%;
    }
  }
</style>
